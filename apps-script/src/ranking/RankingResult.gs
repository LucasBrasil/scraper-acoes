/** Resultado de ranking independente da origem dos scores. */
class RankingResult {
  constructor(number, ranking, score, frequency) {
    this.number = number;
    this.ranking = ranking;
    this.score = score;
    this.frequency = frequency;
  }
}
