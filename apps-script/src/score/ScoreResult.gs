/** Resultado final de score para uma dezena. */
class ScoreResult {
  constructor(number, score, ranking, breakdown, frequency) {
    this.number = number;
    this.score = score;
    this.ranking = ranking;
    this.breakdown = breakdown;
    this.frequency = frequency;
  }
}
